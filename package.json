{
  "name": "website-toolkit",
  "version": "0.2.0",
  "private": true,
  "description": "Local Chrome/Firefox MV3 developer & QA website toolkit.",
  "scripts": {
    "build": "node scripts/build.js",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "lint:manifest": "node -e \"for (const f of ['manifests/chrome.json','manifests/firefox.json']) { const m=JSON.parse(require('fs').readFileSync(f,'utf8')); if(m.manifest_version!==3) throw new Error(f+' not MV3'); } console.log('manifests OK');\""
  }
}
